/*
import 'package:flutter/material.dart';

class MeduButton extends StatelessWidget {
  MeduButton(this.text)
  final String text;
  final String on
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      child: RaisedButton(onPressed: (){}, child: Text("Next")),
    );
  }
}
*/
